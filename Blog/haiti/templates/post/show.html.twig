{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ post.content }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ post.author }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_index') }}">Retour au debut</a>

    <a href="">Vous ne pouvez pas modifier</a>

    
{% endblock %}
